<div class="listings">
		<h2 class="listings_header">カテゴリ一覧</h2>
		<ul class="channel_list">
		<% @categories.each do |category| %>
			<li><%= link_to "#{category.name}", category_path(category) %></li>
		<% end %>
		</ul>
</div>